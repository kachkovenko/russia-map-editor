# Редактор карты России

Статический веб-редактор карты для подготовки графики к презентациям. Работает без бэкенда и после первой загрузки не требует внешних API.

## Что уже работает

- 89 субъектов РФ по статье 65 Конституции РФ;
- множественный выбор регионов и почти 1 000 городов (все административные центры плюс все города от 10 000 жителей), поиск (⌘K) с кнопкой «выбрать всё найденное», блок крупнейших городов, выбор целого федерального округа одной кнопкой; на карте клик выбирает регион, Shift/⌘+клик — несколько, у выбранных можно задать свою заливку независимо от остальных (цвета хранятся по регионам в проекте), Esc снимает выбор;
- вкладка «Выбрано» со всем отмеченным, подсветка объекта на карте при наведении в списке и наоборот;
- однотонная заливка или градиент, редактируемые HEX-коды, положение цветовых точек, прозрачность, отдельный цвет выделения и пять готовых палитр;
- настройка и отключение границ, подписей и городских маркеров (круг, квадрат, ромб, пин); шрифт подписей — шесть встроенных Google Fonts с кириллицей (Inter, Golos Text, Montserrat, PT Sans, PT Serif, Unbounded; OFL, размещены локально) и два системных (Arial, Georgia); размер, толщина и цвет контура текста, цвет линий выноски, контур маркеров (включение и цвет) регулируются; подписи регионов — все, что помещаются, либо только выбранные; подписи городов расставляются без наложений, при нехватке места отодвигаются с линией выноски, любую можно перетащить мышью (двойной клик возвращает на место);
- атласная равновеликая проекция, Меркатор и широкоугольная «Линза» с подвижной точкой обзора: крестик на карте перетаскивается туда, «откуда смотрим» (эта область самая крупная, дальние края компактнее), двойной клик возвращает его в центр; точка хранится в географических координатах и в экспорт не попадает;
- вся карта как объект: клик по пустому месту внутри её габаритов (при наведении подсказывает пунктирная рамка) выделяет карту — рамка с четырьмя углами меняет размер вокруг противоположного угла, ручка сверху поворачивает — карта крутится вокруг центра рамки, и рамка с углами поворачивается вместе с ней (±45° долготы, масштаб и центр сохраняются; в Меркаторе поворота нет), стрелки сдвигают на 1 px, с Shift — на 10; тот же поворот и сила линзового искажения доступны слайдерами;
- без рамки — навигация как на бесконечной доске с точечной сеткой: масштаб колесом, кнопками или щипком на тач-экране и перемещение перетаскиванием, без сдвига интерфейса и без влияния на экспорт;
- отмена и повтор действий (⌘Z / ⇧⌘Z или ⌘Y; навигация по холсту в историю не попадает), защита от случайного сброса;
- рамка слайда 16:9 / 4:3: слайд закреплён на холсте, а те же жесты двигают и масштабируют карту относительно него — так подбирается композиция слайда, которая и уходит в экспорт; всё, что выходит за рамку, приглушается; цвет слайда выбирается, фон при экспорте может быть прозрачным; без рамки экспорт автоматически обрезается по карте;
- экспорт в PNG ×2 (в файл или сразу в буфер обмена), SVG и PPTX, файлы называются `karta-rossii-ГГГГ-ММ-ДД-ЧЧММ.*`; в PowerPoint формат слайда повторяет рамку (без рамки карта вписывается в слайд), карта помещается как векторный SVG с PNG-fallback, а все подписи (города и регионы) — как отдельные редактируемые текстовые объекты с родными атрибутами: шрифт, размер, цвет, выравнивание, контур (эффект «Свечение»); к слайду добавляется заметка об источнике;
- локальное автосохранение настроек, выбора и свёрнутых секций панели в браузере (localStorage);
- адаптивная раскладка: на узких экранах панели объектов и настроек открываются кнопками в шапке;
- переносимый проект `*.project.json`: отдельное сохранение/открытие или автоматическое сохранение вместе с экспортом в одном ZIP-архиве (браузеры блокируют второе скачивание подряд); по галочке в меню экспорта в тот же архив кладётся выбранный встроенный шрифт — оригинальные TTF из Google Fonts, `OFL.txt` и инструкция по установке, чтобы PPTX и SVG открылись с тем же шрифтом на другом компьютере (PptxGenJS шрифты не внедряет).

## Файл проекта и безопасность импорта

Проект — это только версионированные данные оформления, списки выбранных идентификаторов, цвета отдельных регионов и ручные смещения подписей городов. В нём нет HTML, JavaScript, внешних URL или пользовательских файлов. Импорт:

- принимает только `.json` размером до 256 КБ;
- проверяет сигнатуру `map.kachkovenko.kontur` и поддерживаемую версию формата;
- валидирует типы, допустимые значения, числовые диапазоны и цвета (числа вне диапазона обрезаются до границы);
- переносит только заранее известные поля настроек;
- принимает только существующие ID регионов и городов, смещения подписей — только для существующих городов и в разумных пределах, а цвета регионов — только HEX для существующих регионов;
- отклоняет служебные ключи прототипов и никогда не использует `eval` или динамическое выполнение кода.

Неизвестные поля не попадают в состояние приложения. Импортированный файл читается только в браузере и никуда не отправляется.

## Структура

- `index.html`, `app.js`, `styles.css` — приложение без сборки;
- `lib/` — D3, topojson-client, PptxGenJS (локальные копии);
- `data/` — геометрия регионов (`regions.topojson[.js]`, `regions.metadata.json`, происхождение в `REGIONS_DATA.md`) и каталог городов `cities.js`;
- `scripts/` — воспроизводимая сборка и проверка геометрии регионов (`npm run build:regions`, `npm run check:regions`);
- `tools/` — сборка каталога городов из Wikidata;
- `fonts/` — Google Sans для интерфейса и полужирные сабсеты шрифтов подписей (woff2, латиница и кириллица); `fonts/pack/` — неизменённые оригиналы TTF и `OFL.txt` для архива экспорта.

## Локальный запуск

Можно открыть `index.html` напрямую. Для поведения, идентичного публикации:

```bash
python3 -m http.server 8765
```

Откройте `http://localhost:8765`.

## Публикация на GitHub Pages

1. Создайте пустой репозиторий на GitHub и загрузите в него содержимое этой папки.
2. В `Settings → Pages` выберите `Deploy from a branch`, ветку `main`, папку `/ (root)`.
3. Там же в `Custom domain` сначала укажите `map.kachkovenko.com` и сохраните. Файл `CNAME` уже добавлен в проект.
4. После этого у DNS-провайдера создайте CNAME-запись: имя `map`, значение `<ваш-github-логин>.github.io`.
5. Когда GitHub подтвердит DNS и выпустит сертификат, включите `Enforce HTTPS` в настройках Pages.

Альтернатива — Cloudflare Pages или Vercel: импортируйте репозиторий как статический сайт, команда сборки не нужна, output directory — корень проекта. Затем добавьте `map.kachkovenko.com` в настройках доменов сервиса и подтвердите предложенную DNS-запись.

## Источники и ограничения

Перечень субъектов сверяется со статьёй 65 Конституции РФ. Геометрия регионов собрана только из CC0-набора GeoJSON Atlas на базе Natural Earth 1:10m: 85 геометрий из российского слоя и четыре геометрии из слоя первого административного уровня Украины. Исходная версия закреплена по commit и SHA-256; в итоговые файлы не переносятся атрибуты исходного набора. Воспроизводимая сборка и проверки находятся в `scripts/`, полное описание происхождения — в `data/REGIONS_DATA.md`.

Каталог городов собран из Wikidata (CC0): все города от 10 000 жителей внутри 89 субъектов, население по последним доступным данным; пересобирается командой `node tools/build-cities.mjs`. Названия городов ДНР и ЛНР приведены к закреплённым в законодательстве РФ (таблица замен — в скрипте сборки). Используются D3, topojson-client, PptxGenJS (вместе с встроенным в него JSZip — им собирается ZIP при экспорте) и самостоятельно размещённые шрифты Google Fonts (интерфейс — Google Sans, подписи — Inter, Golos Text, Montserrat, PT Sans, PT Serif, Unbounded; все под SIL Open Font License); лицензии перечислены в `THIRD_PARTY_NOTICES.md`.

Крым, Севастополь, Донецкая и Луганская народные республики, Запорожская и Херсонская области показаны в соответствии с законодательством РФ. Их принадлежность и границы международно оспариваются. Геометрия генерализована для обзорной карты и презентаций и не является официальным, кадастровым или юридически значимым описанием границ.

Для пересборки геометрии нужны Node.js и доступ к интернету:

```bash
npm install
npm run build:regions
npm run check:regions
```
